---
category: index
bucket: patterns
generated: true
---

# Patterns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one pattern entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [UNSET sentinel + recursive merge for layered CLI overlays](architecture.md) (search: "## Pattern: UNSET sentinel + recursive merge for layered CLI overlays") - A CLI exposes options that override values in N configuration layers (default config file, project… (~670 tok)
- [Hot-import deferral for slow CLI dependencies](architecture.md) (search: "## Pattern: Hot-import deferral for slow CLI dependencies") - A CLI invocation pays for every top-level `import`/`require` of every module it loads, even when… (~600 tok)
- [Use POSIX-shape paths for every user-visible string](architecture.md) (search: "## Pattern: Use POSIX-shape paths for every user-visible string") - Anywhere a `path.*` result is embedded in CLI output, prompts, audit findings, JSON payloads,… (~230 tok)
- [Model cross-platform PTY launches as pure spawn specs](architecture.md) (search: "## Pattern: Model cross-platform PTY launches as pure spawn specs") - Terminal or runner integrations that must work on native Windows and POSIX shells. (~150 tok)
- [Summary surfaces should use cheap evidence and reserve full proofs for drill-ins](architecture.md) (search: "## Pattern: Summary surfaces should use cheap evidence and reserve full proofs for drill-ins") - Dashboard home cards, aggregate setup summaries, or any overview route that only needs to answer… (~220 tok)
- [Split guardrails by operational decision](architecture.md) (search: "## Pattern: Split guardrails by operational decision") - A safety hook grows multiple policy categories with different risk profiles, self-test corpora,… (~170 tok)
- [Asymmetric trust - set state from output, clear state from input](architecture.md) (search: "## Pattern: Asymmetric trust - set state from output, clear state from input") - Any state machine that classifies streaming PTY / WebSocket / SSE output to track "is the producer… (~400 tok)
- [Pin AWAY from known-bad versions via `!=`, not only `>=`](dependencies.md) (search: "## Pattern: Pin AWAY from known-bad versions via `!=`, not only `>=`") - A dependency you ship in your own `package.json` / `pyproject.toml` constraint is disclosed as… (~830 tok)
- [Error messages MUST include the input identity that caused them](external-lessons.md) (search: "## Pattern: Error messages MUST include the input identity that caused them") -  (2026-05-25; ~700 tok)
- [Bug-fix clusters arc fix → over-correct → calibrate when the original bug was a silent equality-contract violation](external-lessons.md) (search: "## Pattern: Bug-fix clusters arc fix → over-correct → calibrate when the original bug was a silent equality-contract violation") - Three PRs over three days fixing the same root cause (function-as-dedupe-key collisions in… (2026-05-25; ~880 tok)
- [New polymorphic classes ship with silent bugs without integration tests at parity](external-lessons.md) (search: "## Pattern: New polymorphic classes ship with silent bugs without integration tests at parity") - `ContreeEnvironment.execute()` had two `self.session.run(...)` calls back-to-back; every command… (2026-05-25; ~270 tok)
- [Tests that monkeypatch the function under test mask empty production paths](external-lessons.md) (search: "## Pattern: Tests that monkeypatch the function under test mask empty production paths") - CAO shipped `_get_terminal_context()` returning `{"cwd": None, ...}` with a comment claiming… (2026-05-25; ~420 tok)
- [CLI must enforce every constraint the service silently applies](external-lessons.md) (search: "## Pattern: CLI must enforce every constraint the service silently applies") - CAO shipped a CLI key validator that checked charset (`^[a-z0-9-]+$`) but not length, while the… (2026-05-25; ~480 tok)
- [Verify a fix by re-running the original reproducer, not just the test suite](external-lessons.md) (search: "## Pattern: Verify a fix by re-running the original reproducer, not just the test suite") - `Module.load_state` walked `named_parameters()` and applied each value in place. (2026-05-25; ~650 tok)
- [Status markers drift from ground truth unless the audit verifies the underlying change](external-lessons.md) (search: "## Pattern: Status markers drift from ground truth unless the audit verifies the underlying change") - An OIDC workflow change was logged as done across two sessions but the underlying file was never… (2026-05-26; ~1070 tok)
- [Provider message shapes are union types with per-provider ordering constraints](external-lessons.md) (search: "## Pattern: Provider message shapes are union types with per-provider ordering constraints") - Code that wraps LM-provider responses crashes on edge cases the happy-path fixtures never cover. (2026-08-15; ~610 tok)
- [Multi-agent critique - how to run it effectively](multi-agent.md) (search: "## Pattern: Multi-agent critique - how to run it effectively") - Commissioning multiple independent agent reviews of a framework, architecture, or release candidate. (~530 tok)
- [Convert self-declared critique gates into executable checks](multi-agent.md) (search: "## Pattern: Convert self-declared critique gates into executable checks") - A multi-agent critique skill asks sub-agents to declare dimensions, isolation, lens coverage, or… (~310 tok)
- [Delegated-work review before user handoff](multi-agent.md) (search: "## Pattern: Delegated-work review before user handoff") - During the recorded eight-review feedback session, earlier reviewer suggestions had already… (~280 tok)
- [Hold the file-length line continuously, not in a cleanup pass](refactoring.md) (search: "## Pattern: Hold the file-length line continuously, not in a cleanup pass") - A size gate (`size.file-length` in `.gruff-ts.yaml` - threshold 750/warning when the evidence below… (~930 tok)
- [Extract to a new module without a convenience re-export](refactoring.md) (search: "## Pattern: Extract to a new module without a convenience re-export") - Splitting a large file and wanting existing consumers to keep importing from the original path. (~290 tok)
- [Resolve every cut boundary before splicing any of them](refactoring.md) (search: "## Pattern: Resolve every cut boundary before splicing any of them") - Scripting a multi-block extraction where each block's end marker is the next block's start. (~230 tok)
- [Canary-first contract changes (one consumer before all consumers)](refactoring.md) (search: "## Pattern: Canary-first contract changes (one consumer before all consumers)") - Changing a contract that has N consumers (every env class, every audit check, every renderer, every… (~780 tok)
- [Verify structural renames with a repo-wide grep](refactoring.md) (search: "## Pattern: Verify structural renames with a repo-wide grep") - Renaming setup files, moving shared references, or changing canonical doc paths. (~100 tok)
- [Skill consolidation requires a full grep after every merge](refactoring.md) (search: "## Pattern: Skill consolidation requires a full grep after every merge") - Renaming, merging, or deleting skills. (~120 tok)
- [Put prompt side effects on the CLI side of the boundary](refactoring.md) (search: "## Pattern: Put prompt side effects on the CLI side of the boundary") - A prompt contract forbids tracked-file writes or unrestricted I/O, but a new feature needs… (~150 tok)
- [Sandwich-layer refactor for behavior-preserving migration of load-bearing seams](refactoring.md) (search: "## Pattern: Sandwich-layer refactor for behavior-preserving migration of load-bearing seams") - Changing a load-bearing boundary type with many call sites on both sides — a request/response… (~1140 tok)
- [Cross-runner quality-report triage by convergence](verification.md) (search: "## Pattern: Cross-runner quality-report triage by convergence") - `goat-flow quality` runs produce a JSON report per runner. (~1240 tok)
- [Auto-detect required runtime in CI, skip cleanly when absent](verification.md) (search: "## Pattern: Auto-detect required runtime in CI, skip cleanly when absent") - Tests that depend on an external runtime (container engine, language runtime, native binary) may be… (~700 tok)
- [Bounded wait loops in tests, never bare `while not condition`](verification.md) (search: "## Pattern: Bounded wait loops in tests, never bare `while not condition`") - Integration tests that wait for an external state change (server ready, session attached, event… (~540 tok)
- [Verification scope must match change scope](verification.md) (search: "## Pattern: Verification scope must match change scope") - Any change that touches more than just code. (~100 tok)
- [Complexity refactors need file-level lint before closeout](verification.md) (search: "## Pattern: Complexity refactors need file-level lint before closeout") - Reducing complexity in a specific function. (~100 tok)
- [Refactors need typecheck before preflight](verification.md) (search: "## Pattern: Refactors need typecheck before preflight") - After a large extraction or restructuring pass. (~190 tok)
- [Non-gating audit gaps belong in explicit limits](verification.md) (search: "## Pattern: Non-gating audit gaps belong in explicit limits") - A deterministic audit check passes by design, but review evidence shows a reader could… (~230 tok)
- [Source-grep guardrail for banned API surfaces](verification.md) (search: "## Pattern: Source-grep guardrail for banned API surfaces") - A particular API or coding pattern has been identified as dangerous in some scope (`sql.raw` with… (~870 tok)
- [Verification needs a real context boundary](verification.md) (search: "## Pattern: Verification needs a real context boundary") - The same agent writes a change and then proposes to "independently verify" it inside the same… (~170 tok)
- [Phase-boundary PR template for oversize work](workflow.md) (search: "## Pattern: Phase-boundary PR template for oversize work") - A change exceeds practical PR review size (~3000 lines or 20+ files, or GitHub's "exceeds 20,000… (~410 tok)
- [Deny-rule grammar matrix before mirror fanout](workflow.md) (search: "## Pattern: Deny-rule grammar matrix before mirror fanout") - Adding or changing a deny hook rule for an external CLI with subcommands, inherited flags, or… (~370 tok)
- [Dry-run readiness belongs beside the command](workflow.md) (search: "## Pattern: Dry-run readiness belongs beside the command") - A command can write files, launch terminals, mutate harness config, or ask an agent to act. (~160 tok)
- [Skill-playbook structural template](workflow.md) (search: "## Pattern: Skill-playbook structural template") - Authoring a new playbook for `workflow/skills/playbooks/` (browser-use, page-capture,… (~660 tok)
- [Gruff docs cleanup is a tight analyzer loop](workflow.md) (search: "## Pattern: Gruff docs cleanup is a tight analyzer loop") - Fixing `gruff-ts` documentation findings by adding maintainer comments, especially… (~150 tok)
