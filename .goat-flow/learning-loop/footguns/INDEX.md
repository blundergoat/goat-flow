---
category: index
bucket: footguns
generated: true
---

# Footguns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one active footgun entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Agent-produced output may contain control sequences that hijack the host terminal](agent-output-trust.md) (search: "## Footgun: Agent-produced output may contain control sequences that hijack the host terminal") - The agent runs a TUI program (`htop`, `vim`, `tmux`, `nnn`, anything with `\x1b[` escape codes)…
- [Provider message shapes have hard constraints that crash on naive slicing](agent-output-trust.md) (search: "## Footgun: Provider message shapes have hard constraints that crash on naive slicing") - Code that wraps LM-provider responses crashes on edge cases the test fixtures didn't cover.
- [Settings-layer deny globs match guarded phrases quoted inside benign read-only commands](agent-settings.md) (search: "## Footgun: Settings-layer deny globs match guarded phrases quoted inside benign read-only commands") - Read-only Bash calls are denied with `Permission to use Bash with command ... has been denied` and…
- [Installed settings.json deny patterns can silently drift from workflow templates](agent-settings.md) (search: "## Footgun: Installed settings.json deny patterns can silently drift from workflow templates") - An agent can perform an action (e.g.
- [Re-adding a removed agent tool (MultiEdit) reprints "matches no known tool" every launch](agent-settings.md) (search: "## Footgun: Re-adding a removed agent tool (MultiEdit) reprints") - Claude Code printed `Permission deny rule "MultiEdit(/secrets/)" matches no known tool — check for…
- [Audit does not prove end-to-end deny enforcement at runtime](auditor.md) (search: "## Footgun: Audit does not prove end-to-end deny enforcement at runtime") - The selected-agent audit validates hook syntax, self-test behavior, registration, and a…
- [The deny-mechanism runtime smoke executes the target checkout's own hook command](auditor.md) (search: "## Footgun: The deny-mechanism runtime smoke executes the target checkout's own hook command") - The runtime evidence level of the agent deny-mechanism audit does not only run goat-flow's own…
- [Missing directories can false-pass when harness checks use `listDir()` as an existence test](auditor.md) (search: "## Footgun: Missing directories can false-pass when harness checks use `listDir()` as an existence test") - After deleting the old WIP goat-flow install from `api-main`,…
- [Structural Compliance Illusion](auditor.md) (search: "## Footgun: Structural Compliance Illusion") - Build checks in `src/cli/audit/check-goat-flow.ts` and `src/cli/audit/check-agent-setup.ts` prove…
- [Learning-loop record counts have two grammars that disagree on resolved entries](auditor.md) (search: "## Footgun: Learning-loop record counts have two grammars that disagree on resolved entries") - Two surfaces show different counts under the same bucket label.
- [Selected-agent drift can leak unselected agent surfaces](auditor.md) (search: "## Footgun: Selected-agent drift can leak unselected agent surfaces") - Agent selection is easy to preserve in the top-level audit and lose inside a nested drift helper.
- [Extractor diagnostics can encode valid empty state](auditor.md) (search: "## Footgun: Extractor diagnostics can encode valid empty state") - A shared diagnostic channel can carry malformed-metadata errors and valid status such as an empty…
- [TTL'd cache invalidation MUST travel with every writer, not just the writer the bug surfaced from](caching.md) (search: "## Footgun: TTL'd cache invalidation MUST travel with every writer, not just the writer the bug surfaced from") - A read-side cache returns stale data after a write completes.
- [Resource cleanup at one layer leaves the consumer loop running at the next layer](cleanup-layering.md) (search: "## Footgun: Resource cleanup at one layer leaves the consumer loop running at the next layer") - A long-lived resource (container, sandbox, process, session, lock) has its own TTL or cleanup…
- [Session-scoped cleanup over a project-scoped resource deletes a sibling's live state](cleanup-layering.md) (search: "## Footgun: Session-scoped cleanup over a project-scoped resource deletes a sibling's live state") - Two concurrent sessions on one project silently lose work.
- [Host-native paths leak into user-visible CLI output on Windows](cli.md) (search: "## Footgun: Host-native paths leak into user-visible CLI output on Windows") - Windows users see `C:\Users\developer\...` style backslash paths in setup prompts, audit `evidence`…
- [ESM main-module guard breaks under symlinks](cli.md) (search: "## Footgun: ESM main-module guard breaks under symlinks") - CLI exits 0 with zero output.
- [Diagnostic logs to stdout corrupt structured-output modes](cli.md) (search: "## Footgun: Diagnostic logs to stdout corrupt structured-output modes") - A CLI command emits structured output (JSON, SARIF, JSONL, CSV) to stdout for a downstream consumer…
- [Strict validation of a new evidence artifact retroactively fails finished plans](cli.md) (search: "## Footgun: Strict validation of a new evidence artifact retroactively fails finished plans") - A milestone that passed strict validation for weeks starts failing after an unrelated release.
- [Markdown proof gates can promote hidden examples into authority](cli.md) (search: "## Footgun: Markdown proof gates can promote hidden examples into authority") - A review report containing every required field inside a raw `<pre>` block passed validation.
- [Zero-duration filesystem leases can look live at the clock boundary](cli.md) (search: "## Footgun: Zero-duration filesystem leases can look live at the clock boundary") - Identical CI runs at the same commit disagree on `rejects an orphaned stale claim when its draft is…
- [`value || DEFAULT` silently drops valid falsy values (0, "", false)](config.md) (search: "## Footgun: `value || DEFAULT` silently drops valid falsy values (0,") - CLI option documented as "set to 0 to disable" is silently overridden by the config default.
- [Misspelled config key silently disables the feature](config.md) (search: "## Footgun: Misspelled config key silently disables the feature") - A feature reads its configuration from a nested config object via `.get("env", {})` or…
- [`JSON.stringify` as a dedupe key silently drops function values, class instances, and BigInts](config.md) (search: "## Footgun: `JSON.stringify` as a dedupe key silently drops function values, class instances, and BigInts") - A merge or dedupe step over a list of config objects silently loses entries.
- [Project-browser modal is reachable only via header-span click, not from the add-project flow](dashboard.md) (search: "## Footgun: Project-browser modal is reachable only via header-span click, not from the add-project flow") - Users looking for a filesystem-browse capability while adding a project find only a text input.
- [Tailwind utility class names collide with custom component classes](dashboard.md) (search: "## Footgun: Tailwind utility class names collide with custom component classes") - A custom CSS rule appears correct in source but the rendered element has unexpected `box-shadow`,…
- [Native Windows terminal sessions need both a Windows shell plan and a Windows runner shim](dashboard.md) (search: "## Footgun: Native Windows terminal sessions need both a Windows shell plan and a Windows runner shim") - The Workspace view reports `File not found` when `Open terminal` is clicked on native Windows, even…
- [Dashboard reader decoders can erase score-critical API fields](dashboard.md) (search: "## Footgun: Dashboard reader decoders can erase score-critical API fields") - The dashboard can show concern scores, metric notes, or pass/fail labels that disagree with…
- [Dashboard aggregate facts and Home agent cards can use different agent sets](dashboard.md) (search: "## Footgun: Dashboard aggregate facts and Home agent cards can use different agent sets") - Home can show or hide agent cards differently from the aggregate Agent Setup scope.
- [Dashboard terminal helper tests can leak event-loop handles](dashboard.md) (search: "## Footgun: Dashboard terminal helper tests can leak event-loop handles") - A dashboard terminal helper test suite prints passing assertions but the Node test process keeps…
- [Dashboard terminal prompts can be dropped before browser attachment](dashboard.md) (search: "## Footgun: Dashboard terminal prompts can be dropped before browser attachment") - Clicking a dashboard action such as "Run Quality Assessment in Runner" creates a Claude terminal…
- [Workspace terminal waiting state has multiple derived surfaces](dashboard.md) (search: "## Footgun: Workspace terminal waiting state has multiple derived surfaces") - The Workspace header or terminal pane can show a session waiting while the summary meters still…
- [Dashboard agent-targeting uses activeRunner where it should use the failing or selected agent](dashboard.md) (search: "## Footgun: Dashboard agent-targeting uses activeRunner where it should use the failing or selected agent") - The Home "Fix First" card shows a command like `--agent claude` but the agent with the actual…
- [Dashboard-launched Codex access must match the task's write intent](dashboard.md) (search: "## Footgun: Dashboard-launched Codex access must match the task's write intent") - A write-enabled Codex session launched from the Workspace terminal fails `bash…
- [Extension-based secret checks can confuse filenames with query syntax](deny-secrets.md) (search: "## Footgun: Extension-based secret checks can confuse filenames with query syntax") - A secret-path hook correctly blocks `cat path/to/id_rsa.key`, but also blocks harmless jq/yq…
- [File-read deny does not bind Bash shell reads of secret files](deny-secrets.md) (search: "## Footgun: File-read deny does not bind Bash shell reads of secret files") - Before the Bash-side sentinel was added, `goat-flow audit --harness` reported `deny-covers-secrets:…
- [Command-segment splitter must track substitution depth, not just quotes](deny-shell.md) (search: "## Footgun: Command-segment splitter must track substitution depth, not just quotes") - Benign reads denied as `Policy destructive: Complex command substitution`: an unquoted `$(...)`…
- [Nested hook checks must reuse the command segment splitter](deny-shell.md) (search: "## Footgun: Nested hook checks must reuse the command segment splitter") - Pre-fix, the hook blocked top-level `true; rm -rf /` while allowing the same command nested inside…
- [Heredoc masking can hide executable shell lines](deny-shell.md) (search: "## Footgun: Heredoc masking can hide executable shell lines") - Masking quoted heredoc bodies fixes false positives on inert report JSON/prose, but is unsafe if…
- [Dispatcher checks must inspect pipeline segments, not only the whole command](deny-shell.md) (search: "## Footgun: Dispatcher checks must inspect pipeline segments, not only the whole command") - Pre-fix, direct `xargs rm -rf < list.txt` blocked, but equivalent pipelines such as `printf '%s\n'…
- [Shell substitution scanners must be quote-aware inside the substitution body](deny-shell.md) (search: "## Footgun: Shell substitution scanners must be quote-aware inside the substitution body") - A regex-only `$()` / `<()` scanner can stop at a `)` that appears inside a quoted string within the…
- [Splitting a monolithic guardrail can drop parser coverage while preserving the headline checks](deny-shell.md) (search: "## Footgun: Splitting a monolithic guardrail can drop parser coverage while preserving the headline checks") - A hook split looks cleaner because `patterns-shell.sh`, `patterns-paths.sh`, and…
- [Copilot preToolUse hooks must distinguish structured payloads from Bash calls](deny-shell.md) (search: "## Footgun: Copilot preToolUse hooks must distinguish structured payloads from Bash calls") - Copilot `preToolUse` can receive Bash and non-Bash payloads through the same hook.
- [Git push deny checks must normalize shell wrappers and control bodies](deny-writes.md) (search: "## Footgun: Git push deny checks must normalize shell wrappers and control bodies") - A deny hook can appear to block `git push` while allowing valid shell forms that execute a push…
- [GitHub CLI comments bypassed shared-system write guardrails](deny-writes.md) (search: "## Footgun: GitHub CLI comments bypassed shared-system write guardrails") - A model can post to GitHub through `gh issue comment ... --body-file ...` even when `git push` is…
- [Path validators can treat gitignored local-state markers as missing docs](docs-and-crossrefs.md) (search: "## Footgun: Path validators can treat gitignored local-state markers as missing docs") - A clean checkout fails the harness `doc-paths-resolve` check because a committed doc mentions an…
- [Playbooks reference goat-flow repo-internal files absent from consumer installs](docs-and-crossrefs.md) (search: "## Footgun: Playbooks reference goat-flow repo-internal files absent from consumer installs") - A playbook under `workflow/skills/playbooks/` (installed to `.goat-flow/skill-docs/playbooks/`) or…
- [Flipping a doctrine in one playbook leaves siblings citing the old stance](docs-and-crossrefs.md) (search: "## Footgun: Flipping a doctrine in one playbook leaves siblings citing the old stance") - A policy change in one doc passes its own review, but a sibling playbook or instruction file still…
- [Adding an instruction-file section ripples across four section-list sources plus the line target](docs-and-crossrefs.md) (search: "## Footgun: Adding an instruction-file section ripples across four section-list sources plus the line target") - Adding one `## <Section>` heading to an instruction file (e.g.
- [Agent capability metadata goes stale when upstream docs add hooks](docs-and-crossrefs.md) (search: "## Footgun: Agent capability metadata goes stale when upstream docs add hooks") - Dashboard and docs can report an agent as "not supported" for hooks while the runtime has a…
- [Hook additions and renames cross runtime, dashboard, and audit surfaces](docs-and-crossrefs.md) (search: "## Footgun: Hook additions and renames cross runtime, dashboard, and audit surfaces") - A hook script can exist and pass its own smoke test while the dashboard registry, installer,…
- [Active footgun Symptoms paragraph drifts after the underlying bug is fixed](docs-and-crossrefs.md) (search: "## Footgun: Active footgun Symptoms paragraph drifts after the underlying bug is fixed") - A footgun is tagged `Status: active` and reads as a current trap.
- [Adding a skill-playbook requires lock-step updates across 13+ surfaces](docs-and-crossrefs.md) (search: "## Footgun: Adding a skill-playbook requires lock-step updates across 13+ surfaces") - A playbook appears in `workflow/skills/playbooks/` and `.goat-flow/skill-docs/playbooks/`, but one…
- [Cross-reference fragility across docs](docs-and-crossrefs.md) (search: "## Footgun: Cross-reference fragility across docs") - A renamed or moved file breaks links in multiple documents.
- [ADR renumbering breaks cross-references](docs-and-crossrefs.md) (search: "## Footgun: ADR renumbering breaks cross-references") - ADR notes that say "absorbs ADR-NNN" or "supersedes ADR-NNN" can silently point at the wrong…
- [Version bump checks do not cover synthetic project config strings](docs-and-crossrefs.md) (search: "## Footgun: Version bump checks do not cover synthetic project config strings") - The bump and version checks pass while helpers, examples, fixtures, or mirrors still name the…
- [Hot-path agent instructions drift unevenly across agents](docs-and-crossrefs.md) (search: "## Footgun: Hot-path agent instructions drift unevenly across agents") - One agent receives weaker release or routing guidance than the others even though all four…
- [Filesystem-backed validation can miss untracked or ignored replacement files](docs-and-crossrefs.md) (search: "## Footgun: Filesystem-backed validation can miss untracked or ignored replacement files") - Local validation passes, but the next commit or CI run breaks because the replacement file exists…
- [Prose examples for agent-specific paths drift from the manifest](docs-and-crossrefs.md) (search: "## Footgun: Prose examples for agent-specific paths drift from the manifest") - A doc lists an agent-specific path (`.agents/skills/`, `.codex/skills/`, etc.) that does not match…
- [Cold-path docs drift while structural audit passes](docs-drift.md) (search: "## Footgun: Cold-path docs drift while structural audit passes") - The CLI audit reports PASS while cold-path documentation contains false claims, wrong check…
- [Hook toggles can scaffold uninstalled agent surfaces](hook-installation.md) (search: "## Footgun: Hook toggles can scaffold uninstalled agent surfaces") - A hook toggle against a clean target created agent config and hook files for agents the target…
- [Hook command strings can fail before guard code starts](hook-installation.md) (search: "## Footgun: Hook command strings can fail before guard code starts") - Direct hook self-tests pass, but an agent session reports a PreToolUse failure with exit 126 or 127…
- [Hook sync must unignore required policy files](hook-installation.md) (search: "## Footgun: Hook sync must unignore required policy files") - Historical pre-fix failure: `goat-flow hooks enable deny-dangerous` or `goat-flow hooks sync` made…
- [Legacy per-agent hook launchers using --show-toplevel resolve to the worktree, not the main repo](hook-installation.md) (search: "## Footgun: Legacy per-agent hook launchers using --show-toplevel resolve to the worktree, not the main repo") - A Claude or Antigravity session inside a `git worktree add` checkout fails every Bash with a…
- [Hook launchers fail closed when the shell cwd is outside any git repo, wedging every Bash](hook-installation.md) (search: "## Footgun: Hook launchers fail closed when the shell cwd is outside any git repo, wedging every Bash") - A Claude/Antigravity session that `cd`'d outside the repo (usually `/tmp` for scratch) has EVERY…
- [Copilot hook config can exist while runtime policy hooks are disabled](hook-installation.md) (search: "## Footgun: Copilot hook config can exist while runtime policy hooks are disabled") - Goat-flow can verify `.github/hooks/hooks.json` and the deny script while Copilot never invokes the…
- [Codex permission profiles must match the local CLI grammar](hook-installation.md) (search: "## Footgun: Codex permission profiles must match the local CLI grammar") - Codex warns or fails before shell startup when the profile names a workspace-root token, access…
- [Codex config preservation can leave old permission profiles behind](hooks.md) (search: "## Footgun: Codex config preservation can leave old permission profiles behind") - A normal `goat-flow install . --agent codex` upgrade refreshes skills and hook scripts but…
- [Registered Stop hooks can be dead config behind agent trust gates](hooks.md) (search: "## Footgun: Registered Stop hooks can be dead config behind agent trust gates") - Writing a Stop entry into `.codex/hooks.json` or `.agents/hooks.json` does not mean the agent will…
- [Blocking Stop scanners can wedge on gitignored local state](hooks.md) (search: "## Footgun: Blocking Stop scanners can wedge on gitignored local state") - A Claude turn cannot stop even though the tracked/staged repo changes are safe.
- [Per-item subprocess spawning in hooks is ~40x more expensive on Windows Git Bash](hooks.md) (search: "## Footgun: Per-item subprocess spawning in hooks is ~40x more expensive on Windows Git Bash") - A hook is comfortably fast on Linux and unusably slow on Windows Git Bash, with `sys` time near…
- [deny-dangerous re-parses the same command string on every policy check](hooks.md) (search: "## Footgun: deny-dangerous re-parses the same command string on every policy check") - Every Bash tool call carries a visible pause before the command runs, scaling with command…
- [Internal / intermediate runs against a user target must strip side-effect-bearing config](internal-run-isolation.md) (search: "## Footgun: Internal / intermediate runs against a user target must strip side-effect-bearing config") - A user runs a meta-command (optimize, preview, dry-run, dashboard scan, compare) that internally…
- [Quality reviews disappear when the agent skips the final JSON write](quality.md) (search: "## Footgun: Quality reviews disappear when the agent skips the final JSON write") - A quality review ran end-to-end, but `goat-flow quality history` reports no saved runs and…
- [Audit score tempering fields must survive every renderer](quality.md) (search: "## Footgun: Audit score tempering fields must survive every renderer") - A harness concern can truthfully pass while still needing a visible caveat, such as "Verification…
- [Unsupported runtime capabilities must be skipped, not scored missing](quality.md) (search: "## Footgun: Unsupported runtime capabilities must be skipped, not scored missing") - A harness concern can remain capped below 100 for an agent even after all fixable setup work is…
- [Structural validation passes while content is still unanswerable](quality.md) (search: "## Footgun: Structural validation passes while content is still unanswerable") - An audit reports PASS — every required section is present, every heading matches, every structural…
- [Audit checks must not prescribe machine-specific shared content](quality.md) (search: "## Footgun: Audit checks must not prescribe machine-specific shared content") - A deterministic audit check can be technically satisfiable but push users toward committed content…
- [Advisory warnings without enforcement train users to ignore output](quality.md) (search: "## Footgun: Advisory warnings without enforcement train users to ignore output") - A command emits the same wall of warnings on every run, but the warnings never fail the gate and…
- [YAML heredocs can break tooling before shell execution](quality.md) (search: "## Footgun: YAML heredocs can break tooling before shell execution") - A GitHub Actions workflow looks valid as shell, but YAML-aware tools such as Knip fail after an…
- [Pre-release prompts can resolve an older global CLI](quality.md) (search: "## Footgun: Pre-release prompts can resolve an older global CLI") - A prompt generated from the current source tree invokes a newly added CLI command, prints a…
- [`quality diff` reports a finding "resolved" when the next report merely omits it](quality.md) (search: "## Footgun: `quality diff` reports a finding") - `goat-flow quality diff <from>:<to>` shows a non-zero `resolved` count and the reviewer records…
- [A permission mode reused as a feature trigger fires on every session that shares the mode](quality.md) (search: "## Footgun: A permission mode reused as a feature trigger fires on every session that shares the mode") - A feature's setup work runs for sessions that will never use the feature.
- [Sentinel-position policy is invisible until the LM tries trailing output](sentinels.md) (search: "## Footgun: Sentinel-position policy is invisible until the LM tries trailing output") - Submit/completion markers written by an agent at the END of stdout fail to detect when anything…
- [Common code-fence syntax collides with the agent's own work content](sentinels.md) (search: "## Footgun: Common code-fence syntax collides with the agent's own work content") - When the agent's action delimiter is a generic syntax (e.g.
- [Optional-hook agent profiles break when installer treats hooks as universal](setup.md) (search: "## Footgun: Optional-hook agent profiles break when installer treats hooks as universal") - The installer round-trip test can fail for an otherwise valid agent profile with missing hook…
- [New-harness contributions can bypass the manifest-driven installer and modify shared core surfaces](setup.md) (search: "## Footgun: New-harness contributions can bypass the manifest-driven installer and modify shared core surfaces") - A PR proposes "add harness X support." The diff turns out to copy skill files directly into a new…
- [Skill parity edits can miss `.github/skills/` and fail repo-level drift checks](skills.md) (search: "## Footgun: Skill parity edits can miss `.github/skills/` and fail repo-level drift checks") - A skill edit looks complete because `workflow/skills/`, `.agents/skills/`, and `.claude/skills/`…
- [Shared reference edits can split workflow templates from installed runtime copies](skills.md) (search: "## Footgun: Shared reference edits can split workflow templates from installed runtime copies") - An edit to shared skill guidance can look correct in the loaded runtime copy but leave the workflow…
- [Skill reference-pack merges can leave stale installed files behind](skills.md) (search: "## Footgun: Skill reference-pack merges can leave stale installed files behind") - A target project upgraded to the current goat-flow release has current `SKILL.md` files and current…
- [Bash-prescribed slash-command or skill bodies break under per-block tool isolation](skills.md) (search: "## Footgun: Bash-prescribed slash-command or skill bodies break under per-block tool isolation") - A SKILL.md or slash-command body grows past one or two `!cmd` invocations into a multi-block bash…
- [Release-version bumps can break skill-rename work through stale fixtures and hardcoded current-version routing](skills.md) (search: "## Footgun: Release-version bumps can break skill-rename work through stale fixtures and hardcoded current-version routing") - A skill rename can look complete on directory, manifest, and docs surfaces but still fail…
- [New skill proposals can be configuration systems shaped around one workflow rather than general-purpose tools](skills.md) (search: "## Footgun: New skill proposals can be configuration systems shaped around one workflow rather than general-purpose tools") - A thoughtful, first-person, well-written proposal lands for an eighth canonical skill.
- [Linter or security-scanner output can pressure rewrites of load-bearing skill language](skills.md) (search: "## Footgun: Linter or security-scanner output can pressure rewrites of load-bearing skill language") - An automated tool (security scanner, prompt-injection detector, prose linter) flags a phrase or…
