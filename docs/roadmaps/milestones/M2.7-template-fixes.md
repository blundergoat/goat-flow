# Milestone 2.7: Setup + Workflow Template Fixes ~~(COMPLETE)~~

From deep review of `setup/` (10 files) and `workflow/` (26 files) by Claude browser plugin.

## Priority Fixes ~~(ALL FIXED)~~

### 1. setup-claude.md Phase 1b: missing skill template references ~~(FIXED)~~
- [x] Added "Read the detailed skill templates in `workflow/skills/goat-*.md`" to Phase 1b — parity with Codex/Gemini guides

### 2. goat-audit.md: output contradicts constraints ~~(FIXED)~~
- [x] Changed output from "prioritized fix recommendations" to "file:line evidence for each finding" — constraints say "MUST NOT propose fixes"

### 3. ci-validation.md: stale shape-based line targets ~~(FIXED)~~
- [x] Updated to unified 120-line target, hard limit 150 (per ADR-002, shape removed)

### 4. setup/README.md: "Both agents" → "All agents" ~~(FIXED)~~
- [x] Changed to "All agents share:" — was stale from before Gemini support

### 5. mob-elaboration.md: wrong path reference ~~(FIXED)~~
- [x] Changed `workflow/runtime/` to `setup/` — runtime has enforcement templates, not setup prompts

### 6. workflow/README.md created ~~(FIXED)~~
- [x] Maps 6 subdirectories to their purpose, references five-layers.md

## Secondary Fixes

### 7. sbao-ranking.md: missing verification + output format
- **Status:** Deferred — low impact, goat-plan.md skill has the richer SBAO section. Playbook is a reference doc, not the primary interface.

### 8. lessons.md template: invisible format guidance
- **Status:** Deferred — HTML comments are invisible on GitHub but work fine for agents. Low priority cosmetic fix.

### 9. profiles.md: Claude-only in shared directory
- **Status:** Accepted — profiles are Claude-specific by design. Codex documents roles in AGENTS.md. Adding a note would help but isn't blocking.

### 10. Clarify local CLAUDE.md vs .github/instructions/
- **Status:** Resolved by M2.6 — `ai/instructions/` is the source of truth, `.github/instructions/` are Copilot bridges. The old two-approach confusion is eliminated.

### 11. code-map.md + architecture.md outputs not in README
- **Status:** Deferred — cosmetic. These files are discoverable via the router table in each instruction file.

## Observations (from review, no action needed)

- Phase 0 → 1a → 1b → 1c → 2 gated structure is correct
- setup-codex.md honestly documents Codex limitations (~30% compliance)
- setup-gemini.md scope constraint at top (anti-rewrite) is learning from footguns
- shared/ directory avoids 3x duplication
- Step 0 "ask before acting" is highest-value skill design decision
- enforcement.md is the most complete file in the repo
- Feature brief + mob elaboration + SBAO + milestone planning form a complete methodology
- Footgun AI-GENERATED:UNVERIFIED flag with CI enforcement is genuine verification
- Sub-agents work across all three agents (Claude, Codex, Gemini) — SBAO section is correct as-is
