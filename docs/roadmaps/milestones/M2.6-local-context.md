# Milestone 2.6: Local Context — Project Coding Guidelines

**Archetype:** Redesign — replace the removed footgun-heuristic local context checks with project-specific coding guidelines.

## Background

The old local context checks (2.6.1-2.6.3) inferred where local instruction files should exist by counting footgun mentions per directory. Too aggressive — removed in v0.4.0.

Reviewed 6 real projects: all use `.github/instructions/` today, but the files are too file-scoped (e.g., `php.instructions.md`, `strands-rust.instructions.md`). They should be domain-scoped instead (e.g., `backend.md`, `frontend.md`). The vendor-neutral `ai/instructions/` location doesn't exist yet in any project.

## The Pattern

**Hot path / cold path architecture:**

- **Hot path** — `CLAUDE.md`, `AGENTS.md`, `GEMINI.md` — loaded every turn. How the agent should *behave*: execution loop, autonomy tiers, DoD, essential commands. Must stay under 120 lines.
- **Cold path** — `ai/instructions/` — loaded on demand when the task matches. How *this code* works: project-specific coding guidelines, domain conventions, review standards, security constraints. The stuff that doesn't fit in 120 lines but that agents need when working in a particular area.
- **Router** — `ai/README.md` — tells agents which cold-path files to load for which tasks. Precedence order included.

This works in any repo, with any agent, even with no GitHub or Copilot at all. `ai/instructions/` is the local context layer — the project's coding guidelines, organized by domain.

## Structure

```
ai/
├── README.md                    # router — which files to load for which tasks
└── instructions/
    ├── base.md                  # universal project contract
    ├── code-review.md           # review-only guidance
    ├── git-commit.md            # commit format, branch naming, PR workflow
    ├── frontend.md              # frontend domain (React, Vue, etc.)
    ├── backend.md               # backend domain (API, DB, etc.)
    ├── security.md              # cross-cutting security constraints
    └── testing.md               # testing conventions
```

### Domain-based, not file-based

**Wrong** (too granular — scoped to file types):
```
.github/instructions/
├── php.instructions.md          # ← language, not domain
├── python.instructions.md       # ← language, not domain
├── twig.instructions.md         # ← framework template, not domain
├── shell.instructions.md        # ← language, not domain
└── strands-rust.instructions.md # ← library, not domain
```

**Right** (domain-scoped — what the agent needs to know for an area of work):
```
ai/instructions/
├── base.md                      # ← project-wide contract
├── frontend.md                  # ← all frontend: React + Twig + CSS + client-side JS
├── backend.md                   # ← all backend: PHP + Python + API + DB
├── security.md                  # ← cross-cutting: auth, input validation, secrets
└── testing.md                   # ← cross-cutting: test conventions, coverage, mocking
```

A domain file can mention multiple languages. `backend.md` for ambient-scribe would cover PHP (Symfony) AND Python (FastAPI) because the agent working on the backend needs both.

### `.github/` files

Two types of `.github/` files, serving different purposes:

**Universal (any project with `.git/`):**
```
.github/
└── git-commit-instructions.md           # commit format, branch naming — loaded by tools on commit
```

This is not agent-specific. It's for any tool or human that makes commits. Created by every setup guide if `.git/` exists.

**Copilot-only (optional):**
```
.github/
├── copilot-instructions.md              # Copilot HOT PATH — execution loop, autonomy, DoD (like CLAUDE.md)
└── instructions/                        # bridges to ai/instructions/
    ├── frontend.instructions.md         # applyTo: "src/frontend/**" → references ai/instructions/frontend.md
    └── backend.instructions.md          # applyTo: "src/api/**" → references ai/instructions/backend.md
```

`.github/copilot-instructions.md` is a full hot-path file — same structure as `CLAUDE.md`/`AGENTS.md`/`GEMINI.md` (execution loop, autonomy tiers, DoD, essential commands). NOT a bridge. It's Copilot's instruction file.

`.github/instructions/` bridges reference `ai/instructions/` — no duplication of cold-path content.

## Router: `ai/README.md`

```markdown
# Project Coding Guidelines

Read `instructions/base.md` first for every task.

Then load additional files based on the work:

| Task | Load |
|------|------|
| Frontend work | `instructions/frontend.md` |
| Backend work | `instructions/backend.md` |
| Code review | `instructions/code-review.md` |
| Security-sensitive work | `instructions/security.md` |
| Writing tests | `instructions/testing.md` |

Precedence (highest first):
1. security.md (always applies if touching auth/secrets/validation)
2. code-review.md (for review tasks only)
3. domain file (frontend/backend)
4. base.md (always loaded)
```

## Agent Discovery

| Agent | Hot path | Cold path | Discovery |
|-------|----------|-----------|-----------|
| Claude | `CLAUDE.md` | `ai/instructions/` | Router table: `ai/README.md` |
| Codex | `AGENTS.md` | `ai/instructions/` + `.github/instructions/` | Router table + auto-discovery |
| Gemini | `GEMINI.md` | `ai/instructions/` | Router table: `ai/README.md` |
| Copilot | `.github/copilot-instructions.md` | `.github/instructions/` → `ai/instructions/` | Auto-discovers `.github/instructions/` with `applyTo` |
| Cursor / Aider / others | Project README | `ai/instructions/` | `ai/README.md` |

## Setup Guides

All four guides are independent. Each creates `ai/instructions/` if it doesn't exist yet.

| Guide | Hot path | Cold path | `.github/` |
|-------|----------|-----------|------------|
| `setup/setup-claude.md` | Creates `CLAUDE.md` | Creates `ai/instructions/` (if not exists) + router entry | `.github/git-commit-instructions.md` |
| `setup/setup-codex.md` | Creates `AGENTS.md` | Creates `ai/instructions/` (if not exists) + router entry | `.github/git-commit-instructions.md` |
| `setup/setup-gemini.md` | Creates `GEMINI.md` | Creates `ai/instructions/` (if not exists) + router entry | `.github/git-commit-instructions.md` |
| `setup/setup-copilot.md` | Creates `.github/copilot-instructions.md` | Creates `ai/instructions/` (if not exists) + `.github/instructions/` bridges | `.github/git-commit-instructions.md` + bridges |

Every guide creates `.github/git-commit-instructions.md` if `.git/` exists — this is universal, not agent-specific.

Only the Copilot guide creates `.github/copilot-instructions.md` (hot path) and `.github/instructions/` (cold path bridges).

## Standard Files

### Required baseline (scored by scanner)

| File | Purpose | When |
|------|---------|------|
| `ai/README.md` | Router — read this first, tells agents which instruction files to load | Every project |
| `ai/instructions/base.md` | Universal project contract: what the repo is, build/test/lint, conventions, boundaries | Every project |
| `ai/instructions/code-review.md` | Review standards, approval checklist, common anti-patterns | Every project |
| `ai/instructions/git-commit.md` | Commit message format, branch naming, PR workflow | Every project with `.git/` |

### Recommended (not scored)

| File | When to create |
|------|---------------|
| `ai/instructions/frontend.md` | When project has a frontend — framework patterns, component conventions, state management |
| `ai/instructions/backend.md` | When project has a backend — handler patterns, DB conventions, validation, error handling |
| `ai/instructions/security.md` | When project handles auth, user input, secrets, or sensitive data |
| `ai/instructions/testing.md` | When project has test suite — test patterns, coverage expectations, mocking conventions |

### What goes where

**In `base.md` (universal contract):**
- What the repo is
- Architecture overview
- How to run/build/test
- Coding conventions used everywhere
- Generated files (don't edit these)
- Dangerous things not to touch
- Common commands

**In domain files (only what differs from base):**
- Framework-specific patterns (React hooks, Go handler pattern, Symfony services)
- Domain-specific conventions (SQL query style, API versioning)
- Area-specific safety rules (auth boundaries, migration constraints)
- Can mention multiple languages if they're in the same domain

**Key rule:** if the same rule appears in 2+ files, it should be in `base.md` instead.

## Writing Good Instruction Files

- **Concrete and operational** — commands, file paths, do/don't with examples
- **Short** — answer "what do you need to know to safely edit files in this area?" and stop
- **No duplication** — shared rules in base.md, overlays only add or tighten
- **Domain-based, not file-based** — `backend.md` covers all backend languages, not one file per language
- **Specific over general**: "Use `sqlc.arg(name)` in queries" not "write clean SQL"
- **Call out boundaries**: "Never edit `internal/db/*.sql.go`" not "be careful with generated code"

## Tasks

### Phase A: Research + Design (1 session)
1. [ ] Confirm Codex auto-discovery — `.github/instructions/` only, or configurable?
2. [ ] Confirm Claude's behavior with `ai/instructions/` — needs router reference?
3. [ ] Confirm Copilot bridge behavior — does it follow markdown links or need inline content?
4. [ ] Decide: bridge files reference via link or inline content?

### Phase B: Workflow Templates (1 session)
5. [ ] Create `workflow/local-context/README.md` — router template
6. [ ] Create `workflow/local-context/base.md` — template with concrete do/don't examples
7. [ ] Create `workflow/local-context/code-review.md` — review guidance template
8. [ ] Create `workflow/local-context/git-commit.md` — commit format, branch naming, PR workflow template
9. [ ] Create `workflow/local-context/frontend.md` — example domain overlay (multi-language)
10. [ ] Create `workflow/local-context/backend.md` — example domain overlay (multi-language)
11. [ ] Create `workflow/local-context/security.md` — cross-cutting overlay template
12. [ ] Create `workflow/local-context/testing.md` — testing conventions template
13. [ ] Create `workflow/local-context/copilot-bridge.md` — template for `.github/instructions/` bridge files
14. [ ] Create `workflow/local-context/git-commit-github.md` — template for `.github/git-commit-instructions.md`
13. [ ] Update `workflow/local-context/domain-instructions.md` — align with domain-based approach, remove file-based examples
14. [ ] Update `workflow/README.md` — add local-context directory description

### Phase C: Setup + Shared Docs (1-2 sessions)

Every setup guide uses the same two-path logic:

**Path 1: `.github/instructions/` exists → migrate**
1. Read existing `.github/instructions/` files
2. Group by domain (file-based → domain-based: `php.md` + `python.md` → `backend.md`)
3. Create `ai/instructions/` with domain-based files
4. Create `ai/README.md` router
5. Replace `.github/instructions/` content with bridges that reference `ai/instructions/`

**Path 2: nothing exists → create from scratch**
1. Analyze the project (stack, languages, directory structure)
2. Create `ai/README.md` router
3. Create `ai/instructions/base.md` from project analysis
4. Create `ai/instructions/code-review.md` from project conventions
5. Create `ai/instructions/git-commit.md` from detected git workflow
6. Create domain files based on detected stack (frontend.md if React/Vue, backend.md if API/DB, etc.)
7. Create `.github/git-commit-instructions.md` (references `ai/instructions/git-commit.md`) if `.git/` exists

Tasks:
15. [ ] Update `setup/shared/docs-seed.md` — add `ai/` directory creation with two-path logic
16. [ ] Update `setup/shared/execution-loop.md` — reference `ai/instructions/` in router table template
17. [ ] Update `setup/setup-claude.md` Phase 1c — two-path cold path setup, add `ai/README.md` to CLAUDE.md router
18. [ ] Update `setup/setup-codex.md` Phase 1c — two-path cold path setup, add `ai/README.md` to AGENTS.md router
19. [ ] Update `setup/setup-gemini.md` Phase 1c — two-path cold path setup, add `ai/README.md` to GEMINI.md router
20. [ ] Create `setup/setup-copilot.md`:
    - Phase 1a: two-path cold path setup (migrate or create) — same as other guides
    - Phase 1b: create `.github/copilot-instructions.md` — full hot path (execution loop, autonomy tiers, DoD, essential commands)
    - Phase 1c: create `.github/instructions/` bridge files referencing `ai/instructions/`
    - Phase 1d: create `.github/git-commit-instructions.md` (if not exists) — references `ai/instructions/git-commit.md`
    - Phase 2: verify everything resolves
    - Human gate: confirm Copilot Chat picks up instructions
21. [ ] Update `setup/setup-prompt-fix.md` — add `ai/instructions/` to the fix checklist, include migration path
22. [ ] Update `setup/setup-prompt-audit.md` — add `ai/instructions/` to the read list, check for file-based → domain-based issues

### Phase D: Spec + Reference Docs (1 session)
23. [ ] Update `docs/system-spec.md` — add `ai/instructions/` to the artifact table (Layer 5 cold path)
24. [ ] Update `docs/system/five-layers.md` — add cold path layer description
25. [ ] Update `docs/system/six-steps.md` — reference `ai/README.md` in READ step (load domain context)
26. [ ] Update `docs/getting-started.md` — add `ai/instructions/` to the file reference
27. [ ] Update `docs/architecture.md` — mention cold path in system overview
28. [ ] Update `docs/reference/design-rationale.md` — add hot/cold path decision rationale

### Phase E: Scanner Checks (1 session)
29. [ ] Add detection to `facts/shared.ts` — check `ai/instructions/` first, fall back to `.github/instructions/`
30. [ ] `SharedFacts.localInstructions`: `{ dirExists, location, fileCount, files, hasRouter, hasBase, hasCodeReview, hasGitCommit }`
31. [ ] `SharedFacts.gitCommitInstructions`: `{ exists }` — checks `.github/git-commit-instructions.md`
32. [ ] New check 2.6.1: Instructions directory exists — `ai/instructions/` or `.github/instructions/` (1 pt)
33. [ ] New check 2.6.2: Router exists — `ai/README.md` (1 pt)
34. [ ] New check 2.6.3: `base.md` (or `base.instructions.md`) exists (1 pt)
35. [ ] New check 2.6.4: `code-review.md` (or `code-review.instructions.md`) exists (1 pt)
36. [ ] New check 2.6.5: `git-commit.md` (or `git-commit.instructions.md`) exists (1 pt)
37. [ ] New check 2.6.6: `.github/git-commit-instructions.md` exists — if `.git/` exists (1 pt)
38. [ ] Wire into `checkSharedPath()`

### Phase F: Fragments + Tests (1 session)
39. [ ] Fragment: `create-instructions-dir` (kind: create) — `ai/instructions/` + `ai/README.md`
40. [ ] Fragment: `create-base-instructions` (kind: create) — baseline project contract
41. [ ] Fragment: `create-code-review-instructions` (kind: create) — review guidance
42. [ ] Fragment: `create-git-commit-instructions` (kind: create) — commit conventions
43. [ ] Fragment: `create-github-git-commit` (kind: create) — `.github/git-commit-instructions.md`
44. [ ] Fragment: `create-copilot-bridge` (kind: create) — `.github/instructions/` bridge setup
45. [ ] Test: fixture with `ai/instructions/` — checks pass
46. [ ] Test: fixture with `.github/instructions/` — checks pass (both locations work)
47. [ ] Test: fixture without either — checks fail with recommendations
48. [ ] Test: fixture with `.git/` but no `.github/git-commit-instructions.md` — check fails
49. [ ] Fragment coverage test updated

### Phase G: Migration Guide (1 session)
44. [ ] Create `docs/guides/migrate-to-ai-instructions.md` — for existing projects using `.github/instructions/`
45. [ ] Document: how to refactor file-based → domain-based (e.g., `php.instructions.md` + `python.instructions.md` → `backend.md`)
46. [ ] Document: how to set up Copilot bridges from existing `ai/instructions/`

## Exit Criteria

- [ ] Scanner detects both `ai/instructions/` and `.github/instructions/`
- [ ] All four setup guides handle both paths: migrate existing `.github/instructions/` OR create fresh `ai/instructions/`
- [ ] Migration path groups file-based instructions into domain-based files (e.g., `php.md` + `python.md` → `backend.md`)
- [ ] Fresh creation analyzes the project and generates domain files based on detected stack
- [ ] Copilot guide also creates `.github/instructions/` bridge files (references, no duplication)
- [ ] Any guide can run first — no dependency ordering
- [ ] `goat-flow fix .` recommends creating missing instruction files
- [ ] Workflow templates use domain-based organization, not file-based
- [ ] Setup prompts, shared docs, spec docs, and architecture docs all updated
- [ ] All existing tests pass

## Key Decisions

| Decision | Why |
|----------|-----|
| `ai/instructions/` is source of truth | Vendor-neutral. Works for any agent, any platform. |
| `ai/README.md` as router | Discoverable by convention. Tells agents what to load without magic auto-loading. |
| Domain-based, not file-based | `backend.md` > `php.md` + `python.md`. Agents work on domains, not languages. |
| `.github/instructions/` are bridges, not copies | No duplication. One place to update content. |
| `.github/copilot-instructions.md` only for Copilot | Only created by setup-copilot.md. Other agents don't need it. |
| `base.md` + `code-review.md` + `AGENTS.md` as scored baseline | Universal to every project. Domain files are project-specific. |
| All four agents are equal and independent | No dependency between setup guides. Each creates cold path if needed. |

## What M2.6 Does NOT Build

- `applyTo` frontmatter parsing or validation
- Content quality scoring (line count, duplication, specificity)
- Auto-generation of domain files from codebase analysis
- Per-directory `*/CLAUDE.md` or `*/GEMINI.md` scoring
- Copilot Chat integration testing (human gate covers this)
- Automatic migration of existing `.github/instructions/` files (migration guide is manual)
