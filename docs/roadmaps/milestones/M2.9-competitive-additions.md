# Milestone 2.9: Competitive Additions

New skills, infrastructure, and content based on analysis of 33 competitor repos. Validated by 5 independent agent reviews. These additions build on goat-flow's existing moats — they don't replace the core workflow.

## The Structural Gap

```
UNDERSTAND          PLAN            BUILD           VERIFY          LEARN
─────────          ─────           ─────           ──────          ─────
/goat-investigate   /goat-plan      (skill          /goat-test      (footguns.md)
/goat-audit                         chaining,       /goat-review    (lessons.md)
/goat-debug                         not a new       /goat-security  /goat-reflect
/goat-onboard                       skill)          /goat-resume
```

The BUILD column is not a separate skill — it's the execution loop (READ → CLASSIFY → SCOPE → ACT → VERIFY → LOG) guided by skill chaining (M2.8 Phase C). The real gap is **spec artifacts**: goat-plan produces milestones with task checkboxes, but not testable acceptance criteria that goat-review can verify against. "Did we build what we planned?" is currently unanswerable.

## goat-flow Moats (no competitor replicates)

1. Scanner/scoring engine with fragment-based prompt generator
2. Evidence-anchored learning loop (file:line in footguns.md)
3. Hot-path/cold-path instruction architecture (ai/instructions/ split)
4. Cross-reference validation in CI
5. Autonomy tiers with 5-item micro-checklist
6. Multi-agent parity from one framework (4 agents, one codebase)

---

## Phase A: New Skills

### /goat-reflect — Instruction tuning skill (LOW effort)
**Validated by:** Claude Code, RIPER-5, CC-SDD (3 agents)
**Gap:** Learning loop is manual. Competitors auto-extract lessons and update instruction files.
**What it does:** After a session, reads conversation, identifies repeated friction, proposes edits to instruction files. Human approves before changes apply.

**Important distinction:** goat-reflect is **instruction tuning**, not learning loop. It edits CLAUDE.md and ai/instructions/ — NOT footguns.md or lessons.md (those require file:line evidence from real incidents). Behavioral observations from conversations ("I asked about X three times") don't have codebase evidence and don't belong in the learning loop files.

- [ ] Design skill phases:
  - Step 0: "What went well? What was friction? Any repeated mistakes?"
  - Phase 1: Read conversation history — identify repeated questions, friction points, wrong assumptions
  - Phase 2: Check current instruction files — what's missing or misleading that caused friction?
  - Phase 3: Propose specific edits to CLAUDE.md, ai/instructions/, or local instruction files
  - Human gate: "Here's what I'd change in your instruction files. Approve?"
  - MUST NOT auto-edit footguns.md or lessons.md (evidence standards)
- [ ] Create `workflow/skills/goat-reflect.md` template
- [ ] Create `.claude/skills/goat-reflect/SKILL.md`
- [ ] Create `.agents/skills/goat-reflect/SKILL.md`
- [ ] Add to scanner expected skills list

### /goat-onboard — Brownfield codebase onboarding (MEDIUM effort)
**Validated by:** Shinpr Workflows, Claude-Code-Workflows, CC-SDD (3 agents)
**Gap:** goat-flow assumes you're adding the workflow to a project. No help for understanding an unfamiliar codebase.
**What it does:** Systematically maps a codebase: architecture, dependencies, API surface, risk areas. Auto-generates `ai/instructions/` content.

- [ ] Design skill phases:
  - Step 0: "What project? What do you need to understand? Any existing docs?"
  - Phase 1: Detect stack, read manifests, map directory structure
  - Phase 2: Read entry points, follow critical paths, identify boundaries
  - Phase 3: Draft `ai/instructions/` content to temporary location (present inline, NOT auto-populate cold path)
  - Phase 4: Verify generated content against actual code (verification gate)
  - Human gate: "Here's what I'd put in your instruction files — which of these are actually useful?" Human selectively moves approved content to `ai/instructions/`.
  - MUST NOT auto-populate cold path — auto-generated context reduces success by ~3% and increases cost 20%+ (goat-flow's own research)
- [ ] Create `workflow/skills/goat-onboard.md` template
- [ ] Create skill instances
- [ ] Integrate with M2.6 cold path creation (human-gated, not automatic)
- [ ] Add to scanner expected skills list

### /goat-resume — Session continuation (LOW effort)
**Validated by:** Claude-Code-Workflow, Simone, RIPER-5, Spec-Workflow (4 agents)
**Gap:** Long features span multiple sessions. Human manually pastes handoff context.
**What it does:** Reconstructs working state AND proposes next action. Must do more than just read files — it establishes the agent's mental model and suggests what to work on next.

**Skill justification:** Earns skill status because it proposes the next action (not just summarizes). A hook that auto-loads handoff.md doesn't recommend what to do next — the skill does.

- [ ] Design skill phases:
  - Phase 1: Read `tasks/handoff.md` + `tasks/todo.md` + recent `git log --oneline -20`
  - Phase 2: Read current milestone file (if one exists in roadmaps/)
  - Phase 3: Identify: what's in-progress, what's blocked, what's next
  - Phase 4: Present: "Here's where we left off: [task]. Modified files: [list]. I recommend working on [next step] because [reason]. Want to proceed, or re-scope?"
  - Human confirms direction before proceeding
  - If no handoff exists: "No prior session context found. What are you working on?"
- [ ] Create `workflow/skills/goat-resume.md` template
- [ ] Create skill instances
- [ ] Add to scanner expected skills list
- [ ] Consider compaction hook integration (resume logic after compaction)
- [ ] Consider compaction hook integration (resume after compaction)

### Scanner impact — Phase A

Skill count goes from 7 to 10:
- Remove: goat-preflight (M2.8)
- Add: goat-security (M2.8), goat-reflect, goat-onboard, goat-resume (M2.9)

Changes needed:
- [ ] `EXPECTED_SKILLS`: add goat-reflect, goat-onboard, goat-resume
- [ ] Check 2.1.8: "All 10 skills present"
- [ ] CI `required_skills`: add 3 new skills to both `.claude/skills/` and `.agents/skills/` lists
- [ ] Fragments: add `create-skill-reflect`, `create-skill-onboard`, `create-skill-resume`
- [ ] Quality thresholds: review if new skills affect the ratios (reflect is conversational by nature, resume is not phased the same way)

### Scanner impact — Phase B + C

New checks to add after M2.9 is implemented:

| Check | Tier | Pts | What it detects | Phase |
|-------|------|-----|-----------------|-------|
| 2.7.1 | full | 1 | **Spec artifacts exist** — `requirements-*.md` in project root or `docs/requirements/` | Phase C |
| 2.7.2 | full | 1 | **Spec has acceptance criteria** — each requirement has a testable acceptance test | Phase C |
| 2.3.x | standard | 1 | **Audit allowlist exists** — `docs/audit-allowlist.md` (known false positives, prevents re-flagging) | From M2.8 Phase B |
| 3.1.6 | full | 1 | **Eval files are executable** — evals have structured format parseable by eval runner (not just prose) | Phase B |
| 3.1.7 | full | 1 | **Eval pass rate tracked** — eval results file exists with pass/fail counts | Phase B |

Implementation notes:
- [ ] Spec artifact checks (2.7.x) should be N/A until first `requirements-*.md` exists — create-on-first-use pattern
- [ ] Eval executable checks (3.1.6-3.1.7) should be N/A until eval runner exists
- [ ] Audit allowlist check should be N/A until file exists (optional enhancement)
- [ ] All new checks need corresponding fragments in `src/cli/prompt/fragments/`

### Full scanner state after M2.8 + M2.9

```
Current:  80 checks (23 foundation, 38 standard, 19 full) + 9 anti-patterns
After M2.8: ~84 checks (+2 skill quality, +2 from swapped names/updated text)
After M2.9: ~90 checks (+3 skill existence, +5 new full-tier checks)
```

Tier balance after M2.9:
- Foundation: ~23 checks (unchanged — foundation is stable)
- Standard: ~43 checks (+5 from M2.8/M2.9 skill and quality additions)
- Full: ~24 checks (+5 from spec artifacts, evals, allowlist)

---

## Phase B: Infrastructure

### Executable evals (MEDIUM effort)
**Validated by:** Gemini CLI, Superpowers, Ralph (3 agents)
**Gap:** agent-evals/ has manual replay prompts. No automated evaluation.

- [ ] **Architecture decision (decide in doc, not as a task):** Prompt-based with rubric scoring. The agent gets a scenario, and scoring checks: did it gather context? provide file:line evidence? stop at human gate? This matches goat-flow's rubric-as-data philosophy from the scanner. NOT transcript-based (too brittle).
- [ ] Design eval runner based on prompt-based approach
- [ ] Add `goat-flow eval` CLI command
- [ ] Define golden output format for agent-evals/
- [ ] Add eval pass rate to scanner report
- [ ] Document eval design rules (Gemini's approach: deterministic assertions, nightly monitoring, promotion gates)

### GitHub Action — scan on PRs (MEDIUM effort)
**Validated by:** Codex Action, Gemini CLI Action, Copilot (3 agents)
**Gap:** No PR-level quality feedback beyond context-validation.yml.

- [ ] Create `.github/actions/goat-flow-scan/action.yml`
- [ ] Comment score delta on PRs (was A 98%, now A 96% — 2 checks regressed)
- [ ] Block merge if score drops below threshold (--min-score)
- [ ] Publish as reusable GitHub Action
- [ ] **Reconcile with context-validation.yml**: Either the Action replaces it (does everything + scoring) or is additive (scoring + comment, structural checks stay in existing workflow). Define the boundary.

### `goat-flow init` bootstrap (M4 — prioritize first)
**Validated by:** Spec Kit, BMAD, Spec-Workflow, CC-SDD (4 agents)
**Gap:** Users clone/copy/paste. CLI has scan/fix/setup/audit but no init.

- Already planned in `docs/roadmaps/milestones/M4-cli-publish.md`
- Interactive setup: detect stack → create instruction file → create skills → create ai/instructions/ → create hooks
- Should be the first M4 task

---

## Phase C: Content

### Worked examples / case studies (LOW effort)
**Validated by:** Spec Kit, Ralph, 3 agents
- [ ] Create `docs/examples/` directory
- [ ] Add 2-3 case studies: before goat-flow score, after, what changed, time to implement
- [ ] Reference from README.md quick start

### Community surfaces (LOW effort)
**Validated by:** Spec Kit, 2 agents
- [ ] Create `CONTRIBUTING.md` with AI-assistance disclosure policy
- [ ] Create `SECURITY.md`
- [ ] Create `.github/PULL_REQUEST_TEMPLATE.md`
- [ ] Create `CODEOWNERS`

### Spec artifacts from goat-plan (HIGH effort) — HIGHEST LEVERAGE

**Validated by:** Spec Kit, CC-SDD, Spec-Workflow, Claude Code gap analysis (4+ agents)

**Why highest leverage:** This is the connective tissue between plan and review. Without spec artifacts, goat-review can check code quality but can't answer "did we build what we planned?" Competitors with spec-driven development (Spec Kit, CC-SDD, Superpowers) can do two-stage review: spec compliance + code quality. goat-flow currently only does the second.

**Why HIGH effort (not LOW):** Touches 3 skills (plan, review, test) + introduces a new artifact format with traceability. Every competitor that does this (Spec Kit, CC-SDD, Simone) treats it as their core feature. The artifact format needs to be right before downstream consumers are built.

**Approach:** M2.9 adds the `requirements-{feature}.md` artifact to goat-plan ONLY. A separate milestone wires goat-review and goat-test to consume it. Get the format right first.

- [ ] Design `requirements-{feature}.md` format:
  - Each requirement: ID, description, acceptance test (binary pass/fail), linked milestone task
  - Format must be parseable by goat-review's conditional spec compliance phase (M2.8 forward-compatible)
- [ ] Enhance goat-plan Phase 1 to produce this artifact
- [ ] Test: run goat-plan on a real feature, verify requirements are testable
- [ ] Document: goat-review auto-activates spec compliance when this file exists (M2.8 wired this)
- [ ] Future (separate milestone): goat-test gains requirement-derived test cases
- [ ] Future (separate milestone): full traceability: requirement → milestone → implementation → test

---

## Not Adding (validated as not worth it)

| Idea | Why not |
|------|---------|
| Heavy orchestration (Ruflo swarms, Ralph events) | Scope creep — goat-flow is a workflow framework, not an agent platform |
| Skill marketplace/registry | Premature — land `goat-flow init` first |
| Multi-provider model routing | Agent-specific, not a workflow concern |
| Party mode / multi-persona | SBAO in goat-plan handles multi-perspective |
| MCP server mode | Requires runtime shift |
| YOLO mode / autonomous loop | Contradicts safety-first philosophy. Considered but rejected — goat-debug's "no fixes first" gate is the #1 rated feature and YOLO undermines it |
| Translation/i18n | Premature for current adoption |
| Mermaid diagrams | Nice but not core workflow |

---

## Exit Criteria

- [ ] 3 new skills (reflect, onboard, resume) exist in workflow/, .claude/, .agents/
- [ ] Scanner expects 10 skills total
- [ ] `goat-flow eval` command exists and runs agent-evals/ automatically
- [ ] GitHub Action published and tested on goat-flow repo
- [ ] 2+ worked examples in docs/examples/
- [ ] Community surfaces (CONTRIBUTING.md, SECURITY.md) exist
- [ ] goat-plan generates spec artifacts

---

## Dependency Order

```
M2.8 (skill upgrade) must complete first
  → M2.9 Phase A (new skills) — builds on improved skill system
  → M2.9 Phase B (infrastructure) — can run in parallel with Phase A
  → M2.9 Phase C (content) — can start anytime
  → M4 (CLI publish) — goat-flow init is the priority M4 task
```
