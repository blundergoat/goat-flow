# The 6-Step Execution Loop

The complete behavior is defined in `docs/system-spec.md`:

`READ → CLASSIFY → SCOPE → ACT → VERIFY → LOG`

This file is a compatibility pointer for historical references.

- See `docs/system-spec.md` for the canonical loop definition, step problems,
  budgets, and fail behavior.
- Keep this file in sync with the canonical spec when loop wording changes.
